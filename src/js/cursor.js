class Cursor {
    constructor(area) {
        this.area = area
    }

    setDefault(){
        //this.area.style.cursor = 'unset'
    }

    setCustom(url){
        //this.area.style.cursor = `url(${url}), auto`
    }
    
}

export default Cursor